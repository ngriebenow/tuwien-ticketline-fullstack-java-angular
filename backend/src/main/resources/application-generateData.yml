spring:
  profiles:
    include: development
  jpa:
    properties:
      hibernate:
        format_sql: true
        jdbs:
          batch_size: 30
        jdbc:
          time_zone = UTC
        order_inserts: true
        order_updates: true
