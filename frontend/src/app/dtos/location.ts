export class Location {
  constructor(
    public id: number,
    public name: string,
    public street: string,
    public postalCode: string,
    public place: string,
    public country: string
    ) {
  }
}
